import * as catalogActions from './catalog.actions';
import * as blobsActions from './blobs.actions';
import * as manifestsActions from './manifests.actions';
import * as tagsActions from './tags.actions';

export const constants = {
    catalogActions,
    blobsActions,
    manifestsActions,
    tagsActions
};
