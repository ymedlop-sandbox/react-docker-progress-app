import * as CatalogActions from './catalog';
import * as blobsActions from './blobs';
import * as manifestsActions from './manifests';
import * as tagsActions from './tags';

export const RegistryActions = {
    CatalogActions,
    blobsActions,
    manifestsActions,
    tagsActions
};